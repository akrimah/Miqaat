services:
  - type: web
    name: miqaat
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn prayer_times.server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
